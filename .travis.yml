sudo: required
language: python

before_install:
  - docker pull carlad/sinatra

before_script:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" images.sbgenomics.com
  - docker ps -a
  - pip install pyyaml
script:
  - pwd
  - ls -ltra
  - python runtests.py ~/build/markosbg/bunnyCI/ ./rabix.sh
services:
  - docker
