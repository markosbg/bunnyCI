sudo: required
language: python
before_script:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD" images.sbgenomics.com
  - docker ps -a
  - sudo apt-get install python-yaml
script:
  - pwd
  - ls -ltra
  - python runtests.py ~/home/travis/build/markosbg/bunnyCI/ ./rabix.sh
services:
  - docker
